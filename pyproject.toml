[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "project-alpha"
version = "0.1.0"
description = "Portable reference implementation for adaptive marginal-value latent reasoning"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
